type Query {
  families: [ Family ]
  family(familyId: ID!): Family
  offersByFamily(familyId: ID!): [Offer]
  offer(offerId: ID!): Offer
  bundlesByOffer(offerId: ID!): [Bundle]
  bundle(bundleId: ID!): Bundle
  featuresByBundle(bundleId: ID!): [Feature]
  feature(featureId: ID!): Feature
}